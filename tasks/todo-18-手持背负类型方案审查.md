# todo-18 手持背负类型方案审查

## 计划

- [x] 检索与类型扩展相关的 memory、`tasks/lessons.md` 和现有 9 类口径。
- [x] 阅读 `plan/006-新增手持背负类型/006-draft.md`。
- [x] 核对 `src/config/nodeTypeStyle.ts`、`src/domain/topologyParser.ts` 与现有文档基线。
- [x] 整理需要确认的问题并形成结论。

## Review

- `006-draft.md` 的大方向清楚：新增类型 `31`，按 IP 识别，不做高度处理，图例/显隐/标牌/连线颜色都要支持。
- 当前实现已经有一个“特殊类型”分支，即无人机 `30`；再加手持背负 `31` 后，归一化阶段需要从“单一特殊类型判断”升级成“多特殊类型判断”，优先级必须先确认。
- 当前 `001-final.md` 仍是 9 类口径；如果 `31` 落地，总体设计、开发步骤和验收口径都要一起扩到 10 类。
- `public/models/node-icons/svg/手持背负.svg` 文件已存在，但 `006-draft.md` 没给出颜色、形态命名和样式参数，正式实现前需要定口径。
- 用户答复后已明确：
- 先判无人机，再判手持背负。
- `31` 不来自后端 `node_type`，而是在归一化阶段基于 `node_manage_ip_addr` 覆盖得到。
- 显示名定为“手持背负设备”，`shape` 定为“人形”，颜色定为 `#0e932e`，尺寸沿用普通设备一档。
- 同步修改文档，并在确认完成后形成 `006-final.md`。
- 仍需确认：
- `006-answer.md` 第 7 行出现了“高度小于 1 改成 100”的描述，这与 `006-draft.md` 第 5 行“不需要像无人机那样对高度进行任何处理”直接冲突，看起来像误复制的无人机规则，需要明确删除。
- `nodeTypeStyle.ts` 还需要 `modelUri`，但 `006-answer.md` 没指定是沿用哪一个现有 GLB 占位模型。
- `手持背负.svg` 当前仍是下载型大坐标系 SVG，若直接使用，大概率会像之前的 `无人机.svg` 一样出现视觉上偏小的问题，最好在正式实现前统一到现有 `40x40` 画布风格。
