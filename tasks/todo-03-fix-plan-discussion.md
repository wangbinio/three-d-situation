# 003 问题修复计划讨论

## 目标

- 核对 `plan/003-问题修复/003-draft.md` 中 10 个问题在当前代码中是否存在。
- 对照 `plan/001-总体设计/001-final.md` 查找可能遗漏的问题。
- 先输出讨论清单，待确认后再生成 `plan/003-问题修复/fix-steps.md` 与 `plan/003-问题修复/phases/phase-*.md`。

## 待办

- [x] 读取修复草稿，确认本轮任务边界。
- [x] 阅读总体设计文档，提取 UI、图标、图例、轨迹、链路、状态展示相关要求。
- [x] 检查当前代码实现，定位相关组件、状态与渲染逻辑。
- [x] 逐项判断 10 个问题是否存在，并记录证据。
- [x] 对照设计文档补充遗漏问题。
- [x] 输出讨论清单，等待确认。
- [x] 讨论完成后生成修复步骤索引与阶段计划文件。

## Review

- 已确认草稿 10 项中，标题、节点圆点图标、图标尺寸、状态外圈、图例交互、底部工具栏、Cesium credit、线宽均为当前实现问题。
- 图例“一列”问题需细化：当前列表是单列，但每行内部仍包含形状文字和节点名称两列式信息，不符合“只要图标 - 节点目标类型”。
- 历史轨迹 720 点与可配置已实现；缺失的是 `1e-8` 类阈值去重，目前只跳过完全相等坐标。
- 额外发现：配置引用了 `/models/node-icons/*.glb`，但仓库内没有实际 GLB/GLTF 资源；即使切换到 `Entity.model` 也会缺资产。
- 用户已确认：允许生成 8 个 GLB 图标资产；历史航迹线宽 `1`、链路线宽 `1`；两个显示复选框默认勾选；离线节点仅针对后端仍返回且 `node_status=0` 的节点显示黑色外圈。
- 已生成 `plan/003-问题修复/fix-steps.md` 与 `plan/003-问题修复/phases/phase-00.md` 至 `phase-05.md`。
- 已按阶段完成实现、完整验证和提交；最终验证记录见 `docs/verification-report.md` 的“003 问题修复最终验收”。
