1. 草案中的判断存在诸多问题。现在统一简化，只要是无人机类型，并且高度小于1，那么就将高度置为100。历史轨迹size判断，距离判断都不需要。
  
2. 在归一化阶段统一覆盖，防止前后层会出现类型不一致。

3. 在归一化阶段就完成高度处理，也就是json解析完之后，就处理高度，等于是变相调整了json中的高度，如果小于1，就是100。

4. 用了上面的规则，应该不会跳变了。

5. 需要一起调整图例数量、显隐键值和测试基线；否则“固定 8 类”的设计和现有测试都会失真。

6. ip范围不好配置，能不能做成一个一个配置的，比如172.16.6.11-172.16.6.18，落到配置中就是：
   ```
   172.16.6.11
   172.16.6.12
   172.16.6.13
   172.16.6.14
   172.16.6.15
   172.16.6.16
   172.16.6.17
   172.16.6.18
   ```

   但是如果在代码中硬编码更简单那就硬编码。

7. 不需要距离判断了。

8. 修改完之后要同步文档。
